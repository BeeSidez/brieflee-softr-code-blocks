// =====================================================================
// Vibe Coding block: Homepage · Plan 5b · Storyboard
// =====================================================================
// Standalone block for the Storyboard module — drops in below the
// Briefs block (plan.jsx). Cards are alternated: VISUAL on the LEFT,
// TEXT on the RIGHT (opposite of Briefs in plan.jsx).
//
// Section header (Plan eyebrow + "Brief creators on what works."
// headline) lives in plan.jsx — this block intentionally has NO
// section header, just the module's cards. Same HERO_BG section
// background so the two blocks read as one continuous section.
//
// SOFTR UI SETUP:
//   1. Source tab → none required.
//   2. Visibility tab → public.
// =====================================================================

const NAVY = "#001364";
const PERI = "#879CF7";

const HERO_BG =
  "radial-gradient(ellipse 65% 50% at 50% 50%, rgba(135,156,247,0.28) 0%, rgba(180,192,245,0.14) 40%, rgba(180,192,245,0) 75%), #FAFBFF";

const SIGNUP_URL = "https://www.brieflee.co/signup";

// ----- Module data (Storyboard) --------------------------------------
const MODULE = {
  name: "Storyboard",
  icon: "https://res.cloudinary.com/dchroynzv/image/upload/brieflee_icon_storyboard-clapperboard-glass-3d-transparent_2026-05.png",
  headline: "Frame by frame, beat by beat.",
  body: "Drop any TikTok, Reel, or Short. Brieflee maps every visual, line, text overlay, and cut.",
  bullets: ["Read it like a script.", "Edit any beat without re-sending."],
  cta: { label: "Start free trial", href: SIGNUP_URL },
  mockup: "https://res.cloudinary.com/dchroynzv/image/upload/v1778599097/brieflee_feature-card_storyboard-3-frames-glow-for-it-transparent_2026-05.png",
  mockupAlt: "Three-frame storyboard breakdown of a skincare creator video",
};

// =====================================================================
// Block
// =====================================================================
export default function Block() {
  return (
    <section
      className="relative w-full py-8 md:py-10 lg:py-12 px-5 md:px-10"
      style={{
        background: HERO_BG,
        fontFamily: "'Plus Jakarta Sans', system-ui, sans-serif",
      }}
    >
      <Keyframes />

      <div className="max-w-6xl mx-auto">
        <div className="plan-row">
          {/* LEFT — visual card (alternated: visual first this time) */}
          <div className="visual-card shimmer-card">
            <img
              src={MODULE.mockup}
              alt={MODULE.mockupAlt}
              draggable={false}
              className="plan-mockup"
            />

            {/* Glass UI overlay 1 — top toolbar with storyboard tabs */}
            <div className="glass-toolbar">
              <div className="glass-tool active">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
                <span>Beats</span>
              </div>
              <div className="glass-tool">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
                <span>Script</span>
              </div>
              <div className="glass-tool">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg>
                <span>Audio</span>
              </div>
              <div className="glass-divider" />
              <div className="glass-tool icon-only">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round"><circle cx="12" cy="12" r="1"/><circle cx="19" cy="12" r="1"/><circle cx="5" cy="12" r="1"/></svg>
              </div>
            </div>

            {/* Glass UI overlay 2 — beat timeline panel */}
            <div className="glass-panel">
              <div className="glass-panel-row"><span className="glass-check on" />Hook <span className="glass-tag">0:00</span></div>
              <div className="glass-panel-row"><span className="glass-check on" />Story 1 <span className="glass-tag">0:03</span></div>
              <div className="glass-panel-row"><span className="glass-check on" />Story 2 <span className="glass-tag">0:08</span></div>
              <div className="glass-panel-row"><span className="glass-check on" />End <span className="glass-tag">0:14</span></div>
            </div>
          </div>

          {/* RIGHT — text card (second this time) */}
          <div className="text-card shimmer-card">
            <img
              src={MODULE.icon}
              alt={`${MODULE.name} icon`}
              draggable={false}
              className="plan-icon"
            />
            <div className="mt-5">
              <ModuleEyebrow>{MODULE.name}</ModuleEyebrow>
            </div>
            <h3
              className="mt-5 text-2xl md:text-3xl lg:text-[34px] font-bold tracking-tight leading-[1.12] text-balance"
              style={{ color: NAVY }}
            >
              {MODULE.headline}
            </h3>
            <p
              className="mt-4 text-base leading-relaxed font-medium"
              style={{ color: NAVY, opacity: 0.72 }}
            >
              {MODULE.body}
            </p>
            <ul className="plan-bullets">
              {MODULE.bullets.map((b, i) => (
                <li key={i}>
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke={PERI} strokeWidth="3" strokeLinecap="round" strokeLinejoin="round">
                    <polyline points="20 6 9 17 4 12" />
                  </svg>
                  <span>{b}</span>
                </li>
              ))}
            </ul>
            <div className="mt-7">
              <PrimaryCta label={MODULE.cta.label} href={MODULE.cta.href} />
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}

// ─── Module eyebrow (small variant) ──────────────────────────────────
function ModuleEyebrow({ children }) {
  return (
    <div
      style={{
        display: "inline-flex",
        alignItems: "center",
        gap: 8,
        padding: "6px 12px",
        background: "rgba(135,156,247,0.16)",
        color: NAVY,
        fontSize: 11,
        fontWeight: 800,
        letterSpacing: "0.10em",
        textTransform: "uppercase",
        borderRadius: 999,
      }}
    >
      <span style={{ width: 6, height: 6, borderRadius: "50%", background: PERI, boxShadow: `0 0 0 3px ${PERI}44` }} />
      {children}
    </div>
  );
}

function PrimaryCta({ label, href }) {
  return (
    <a
      href={href}
      style={{
        display: "inline-flex",
        alignItems: "center",
        gap: 9,
        padding: "13px 22px",
        borderRadius: 12,
        background: PERI,
        color: "#ffffff",
        fontSize: 15,
        fontWeight: 700,
        textDecoration: "none",
        boxShadow:
          "0 2px 0 rgba(0,0,0,0.14), 0 12px 24px -10px rgba(135,156,247,0.55)",
      }}
    >
      {label}
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
        <path d="M5 12h14M13 5l7 7-7 7" />
      </svg>
    </a>
  );
}

// ─── Keyframes + shared CSS ──────────────────────────────────────────
function Keyframes() {
  return (
    <style>{`
      /* ─── Row · TWO cards side by side with a gap ─────────────── */
      .plan-row {
        display: flex;
        flex-direction: column;
        gap: 16px;
        align-items: stretch;
      }
      @media (min-width: 768px) {
        .plan-row {
          flex-direction: row;
          gap: 20px;
          min-height: 540px;
        }
      }

      /* ─── Shimmer-outline cards · transparent fill, animated outline */
      @property --shimmer-angle {
        syntax: '<angle>';
        initial-value: 0deg;
        inherits: false;
      }
      @keyframes shimmer-rotate {
        to { --shimmer-angle: 360deg; }
      }
      .shimmer-card {
        position: relative;
        background: transparent;
      }
      .shimmer-card::before {
        content: '';
        position: absolute;
        inset: 0;
        border-radius: inherit;
        padding: 2.5px;
        background: conic-gradient(
          from var(--shimmer-angle, 0deg),
          rgba(135,156,247,0.18) 0deg,
          rgba(135,156,247,0.18) 200deg,
          rgba(135,156,247,0.85) 250deg,
          #ffffff 280deg,
          rgba(135,156,247,0.85) 310deg,
          rgba(135,156,247,0.18) 360deg
        );
        -webkit-mask:
          linear-gradient(#000 0 0) content-box,
          linear-gradient(#000 0 0);
        -webkit-mask-composite: xor;
                mask-composite: exclude;
        pointer-events: none;
        animation: shimmer-rotate 3.5s linear infinite;
      }

      /* ─── LEFT card · visual card (WIDER) ─────────────────────── */
      .visual-card {
        flex: 1;
        background: transparent;
        border-radius: 28px;
        padding: 20px 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 320px;
        overflow: hidden;
        position: relative;
      }
      @media (min-width: 768px) {
        .visual-card {
          flex: 1.5;
          padding: 24px 20px;
        }
      }

      /* ─── RIGHT card · text card (narrower) ───────────────────── */
      .text-card {
        flex: 1;
        background: transparent;
        border-radius: 28px;
        padding: 36px 28px;
        display: flex;
        flex-direction: column;
      }
      @media (min-width: 768px) {
        .text-card {
          flex: 1;
          padding: 56px 48px;
        }
      }
      /* Stagger the text card's shimmer so the two cards aren't in
         lockstep — looks more alive */
      .text-card.shimmer-card::before { animation-delay: -2.5s; }

      /* Big 3D glass icon — hover wiggle */
      .plan-icon {
        width: 120px;
        height: 120px;
        object-fit: contain;
        display: block;
        transform-origin: center 70%;
        transition: transform 0.3s ease-out;
      }
      @media (min-width: 768px) {
        .plan-icon { width: 180px; height: 180px; }
      }
      .text-card:hover .plan-icon {
        animation: icon-wiggle 0.9s cubic-bezier(.36,.07,.32,1.5);
      }
      @keyframes icon-wiggle {
        0%   { transform: rotate(0deg) translateY(0) scale(1); }
        15%  { transform: rotate(-10deg) translateY(-6px) scale(1.06); }
        30%  { transform: rotate(8deg)  translateY(-4px) scale(1.08); }
        45%  { transform: rotate(-6deg) translateY(-5px) scale(1.06); }
        60%  { transform: rotate(4deg)  translateY(-3px) scale(1.04); }
        75%  { transform: rotate(-2deg) translateY(-1px) scale(1.02); }
        100% { transform: rotate(0deg)  translateY(0)    scale(1); }
      }

      .plan-bullets {
        list-style: none;
        padding: 0;
        margin: 22px 0 0;
        display: flex;
        flex-direction: column;
        gap: 10px;
      }
      .plan-bullets li {
        display: flex;
        align-items: flex-start;
        gap: 10px;
        font-size: 15px;
        font-weight: 600;
        color: ${NAVY};
        opacity: 0.85;
      }
      .plan-bullets li svg {
        margin-top: 4px;
        flex-shrink: 0;
      }

      /* Mockup image inside the visual card */
      .plan-mockup {
        width: 100%;
        height: auto;
        display: block;
        border-radius: 16px;
        position: relative;
        z-index: 1;
      }

      /* ─── Glass UI overlays · floating on top of the mockup ──── */
      .glass-toolbar {
        position: absolute;
        top: 14%;
        left: 50%;
        transform: translateX(-50%);
        z-index: 3;
        display: inline-flex;
        align-items: center;
        gap: 4px;
        padding: 6px;
        border-radius: 14px;
        background: linear-gradient(135deg, rgba(255,255,255,0.62) 0%, rgba(244,246,255,0.42) 100%);
        -webkit-backdrop-filter: blur(20px) saturate(220%);
        backdrop-filter: blur(20px) saturate(220%);
        border: 1px solid rgba(255,255,255,0.55);
        box-shadow:
          0 1px 0 0 rgba(255,255,255,0.95) inset,
          0 -1px 0 0 rgba(0,19,100,0.08) inset,
          0 12px 28px -10px rgba(0,19,100,0.22);
      }
      .glass-tool {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 7px 10px;
        border-radius: 9px;
        font-size: 11px;
        font-weight: 700;
        color: ${NAVY};
        cursor: pointer;
        white-space: nowrap;
      }
      .glass-tool.active {
        background: ${PERI};
        color: #fff;
        box-shadow: 0 2px 6px -1px rgba(135,156,247,0.5);
      }
      .glass-tool.icon-only { padding: 7px; }
      .glass-divider {
        width: 1px;
        height: 18px;
        background: rgba(0,19,100,0.12);
        margin: 0 2px;
      }

      .glass-panel {
        position: absolute;
        top: 32%;
        right: 8%;
        z-index: 3;
        padding: 10px 12px;
        border-radius: 12px;
        background: linear-gradient(135deg, rgba(255,255,255,0.62) 0%, rgba(244,246,255,0.42) 100%);
        -webkit-backdrop-filter: blur(20px) saturate(220%);
        backdrop-filter: blur(20px) saturate(220%);
        border: 1px solid rgba(255,255,255,0.55);
        box-shadow:
          0 1px 0 0 rgba(255,255,255,0.95) inset,
          0 -1px 0 0 rgba(0,19,100,0.08) inset,
          0 16px 36px -12px rgba(0,19,100,0.22);
        display: flex;
        flex-direction: column;
        gap: 6px;
        min-width: 168px;
      }
      .glass-panel-row {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 11px;
        font-weight: 600;
        color: ${NAVY};
      }
      .glass-check {
        width: 14px;
        height: 14px;
        border-radius: 4px;
        background: rgba(0,19,100,0.06);
        border: 1px solid rgba(0,19,100,0.10);
        flex-shrink: 0;
        display: inline-flex;
        align-items: center;
        justify-content: center;
      }
      .glass-check.on {
        background: ${PERI};
        border-color: ${PERI};
      }
      .glass-check.on::after {
        content: '';
        width: 7px;
        height: 4px;
        border-left: 1.6px solid #fff;
        border-bottom: 1.6px solid #fff;
        transform: rotate(-45deg) translate(0, -1px);
      }
      .glass-tag {
        margin-left: auto;
        padding: 2px 6px;
        border-radius: 5px;
        background: rgba(0,19,100,0.08);
        font-size: 9px;
        font-weight: 800;
        letter-spacing: 0.04em;
        color: ${NAVY};
      }

      /* Hide the second overlay on phones */
      @media (max-width: 639px) {
        .glass-panel { display: none; }
        .glass-toolbar { top: 8%; padding: 4px; }
        .glass-tool { padding: 5px 8px; font-size: 10px; }
      }

      /* Tablet (sm-lg, 640-1023px) */
      @media (min-width: 640px) and (max-width: 1023px) {
        .text-card { padding: 36px 28px; }
        .visual-card { padding: 32px 20px; }
        .plan-icon { width: 120px; height: 120px; }
      }

      /* Mobile (<640px): cards stack vertically, icon smaller */
      @media (max-width: 639px) {
        .visual-card { min-height: 260px; padding: 28px 20px; }
        .plan-icon { width: 96px; height: 96px; }
      }
    `}</style>
  );
}
